{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
{{- range .Data.Pages }}
  {{- if not (in (slice "tags" "categories") .Section) }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- with .Lastmod }}
    <lastmod>{{ .Format "2006-01-02T15:04:05-07:00" | safeHTML }}</lastmod>
    {{- end }}
    {{- $changefreq := "monthly" }}
    {{- if .IsHome }}{{ $changefreq = "weekly" }}{{ end }}
    {{- if eq .Section "posts" }}{{ $changefreq = "monthly" }}{{ end }}
    <changefreq>{{ $changefreq }}</changefreq>
    {{- $priority := 0.5 }}
    {{- if .IsHome }}{{ $priority = 1.0 }}{{ end }}
    {{- if in (slice "/about/" "/gdg/" "/projects/" "/posts/" "/newsletter/") .RelPermalink }}{{ $priority = 0.8 }}{{ end }}
    {{- if and (.IsPage) (eq .Section "projects") }}{{ $priority = 0.7 }}{{ end }}
    {{- if and (.IsPage) (eq .Section "posts") }}{{ $priority = 0.6 }}{{ end }}
    <priority>{{ $priority }}</priority>
  </url>
  {{- end }}
{{- end }}
</urlset>
